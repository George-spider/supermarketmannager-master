server:
  port: 80
  servlet:
      context-path: / #默认的 - 只要是默认都可以不用写
#连接数据库的四个必要参数
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: smm
    password: 123456
    url: jdbc:mysql://124.70.129.121:3306/smm?useUnicode=true&characterEncoding=utf-8&useSSL=true
mybatis:
  type-aliases-package: com.jd.domain #配置别名
  #mapper-locations: classpath:cn/itsource/mapper/*Mapper.xml #约定大于配置 - 可以不写
logging:
  level:
    com.jd: trace #配置日志